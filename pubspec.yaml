name: fitnessapp
description: Fitness tracking app
publish_to: "none"
version: 1.0.0+1

environment:
  sdk: ">=3.2.0 <4.0.0"

dependencies:
  flutter:
    sdk: flutter

  # Firebase
  firebase_core: ^3.6.0
  firebase_auth: ^5.3.1
  firebase_app_check: ^0.3.1
  cloud_firestore: ^5.4.4
  cloud_functions: ^5.6.2
  firebase_storage: ^12.3.3
  firebase_messaging: ^15.1.3   # optional, remove if not using

  # THIS WAS YOUR BUG — FIXED BELOW
  flutter_localizations:
    sdk: flutter

  # UI & Utils
  flutter_dotenv: ^5.1.0
  fl_chart: ^0.69.0
  lottie: ^3.1.2
  cupertino_icons: ^1.0.8
  share_plus: ^7.2.2
  path_provider: ^2.1.2
  intl: ^0.20.2
  image_picker: ^1.1.2
  url_launcher: ^6.3.0
  provider: ^6.1.5
  uuid: ^4.5.0
  shared_preferences: ^2.3.2
  cached_network_image: ^3.3.1
  device_info_plus: ^10.1.0
  http: ^1.2.2
  google_generative_ai: ^0.4.4
  just_audio: ^0.9.38  # For music & sounds
  confetti: ^0.7.0  # For end celebration
  video_player: ^2.8.2  # ADD THIS LINE


dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^4.0.0

flutter:
  generate: true
  uses-material-design: true

  assets:
    - assets/sounds/
    - assets/images/

    - assets/videos/

